import type { APISettingsInput, VoiceEngine } from '..';
type APISettings = APISettingsInput & {
    defaultVoiceId: string;
    defaultVoiceEngine: VoiceEngine;
};
export declare class APISettingsStore {
    settings: APISettings;
    private static _instance;
    private constructor();
    static getSettings(): APISettings;
    static setSettings(settings: APISettingsInput): void;
}
export {};
